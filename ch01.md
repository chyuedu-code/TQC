# TQC 程式設計題庫練習

## 綜合範例 1-1：

請撰寫一程式，輸入四個整數，然後將這四個整數以欄寬為 5、每列印兩個的方式，先列印向右靠齊，再列印向左靠齊，左右皆以直線 | (Vertical bar) 作為邊界。

### 輸入與輸出樣本：

**輸入：**
```text
85
4
299
478
```
**輸出：**
```text
|   85     4|
|  299   478|
|85    4    |
|299   478  |
```
---

## 綜合範例 1-2：

請撰寫一程式，輸入四個分別含有小數 1 到 4 位的浮點數，然後將這四個浮點數以欄寬為 7、每列印兩個的方式，先列印向右靠齊，再列印向左靠齊，左右皆以直線 | (Vertical bar) 作為邊界。

> 💡 **提示**：輸出浮點數到小數點後第二位。

### 輸入與輸出樣本：

**輸入：**
```text
23.12
395.3
100.4617
564.329
```
**輸出：**
```text
|  23.12  395.30|
| 100.46  564.33|
|23.12   395.30 |
|100.46  564.33 |
```
---

## 綜合範例 1-3：

請撰寫一程式，輸入四個單字，然後將這四個單字以欄寬為 10、每列印兩個的方式，先列印向右靠齊，再列印向左靠齊，左右皆以直線 | (Vertical bar) 作為邊界。

### 輸入與輸出樣本：

**輸入：**
```text
I
enjoy
learning
Python
```
**輸出：**
```text
|         I     enjoy|
|  learning    Python|
|I         enjoy     |
|learning  Python    |
```
---

## 綜合範例 1-4：

請撰寫一程式，輸入一圓的半徑，並加以計算此圓之面積和周長，最後請印出此圓的半徑（Radius）、周長（Perimeter）和面積（Area）。

> 💡 **提示**：1：需 import math 模組，並使用 math.pi。
> 💡 **提示**：2：輸出浮點數到小數點後第二位。

### 輸入與輸出樣本 1：

**輸入：**
```text
10
```
**輸出：**
```text
Radius = 10.00
Perimeter = 62.83
Area = 314.16
```
### 輸入與輸出樣本 2：

**輸入：**
```text
2.5
```
**輸出：**
```text
Radius = 2.50
Perimeter = 15.71
Area = 19.63
```
---

## 綜合範例 1-5：

請撰寫一程式，輸入兩個正數，代表一矩形之高和寬，計算並輸出此矩形之高（Height）、寬（Width）、周長（Perimeter）及面積（Area）。

> 💡 **提示**：輸出浮點數到小數點後第二位。

### 輸入與輸出樣本：

**輸入：**
```text
23.5
19
```
**輸出：**
```text
Height = 23.50
Width = 19.00
Perimeter = 85.00
Area = 446.50
```
---

## 綜合範例 1-6：

假設一賽跑選手在 x 分 y 秒的時間跑完 z 公里，請撰寫一程式，輸入 x、y、z 數值，最後顯示此選手每小時的平均英哩速度（1 英哩等於 1.6 公里）。

> 💡 **提示**：輸出浮點數到小數點後第一位。

### 輸入與輸出樣本：

**輸入：**
```text
10
25
3
```
**輸出：**
```text
Speed = 10.8
```
---

## 綜合範例 1-7：

請撰寫一程式，讓使用者輸入五個數字，計算並輸出這五個數字之數值、總和以及平均數。

> 💡 **提示**：輸出到小數點後第一位。

### 輸入與輸出樣本 1：

**輸入：**
```text
20
40
60
80
100
```
**輸出：**
```text
20 40 60 80 100
Sum = 300.0
Average = 60.0
```
### 輸入與輸出樣本 2：

**輸入：**
```text
88
12
56
132.55
3
```
**輸出：**
```text
88 12 56 132.55 3
Sum = 291.6
Average = 58.3
```
---

## 綜合範例 1-8：

請撰寫一程式，讓使用者輸入四個數字 x1、y1、x2、y2，分別代表兩個點的座標 (x1, y1)、(x2, y2)。計算並輸出這兩點的座標與其歐式距離。

> 💡 **提示**：歐式距離 = √((x1 - x2)² + (y1 - y2)²) 。

### 輸入與輸出樣本：

**輸入：**
```text
2
1
5.5
8
```
**輸出：**
```text
( 2, 1 )
( 5.5, 8 )
Distance = 7.826237921249264
```
---

## 綜合範例 1-9：

請撰寫一程式，讓使用者輸入一個正數 s，代表正五邊形之邊長，計算並輸出此正五邊形之面積（Area）。

> 💡 **提示**：1：需 import math 模組，並使用 math.pow 及 math.tan。
> 💡 **提示**：正五邊形面積的公式：Area = (5 * s²) / (4 * tan(pi/5))。

### 輸入與輸出樣本：

**輸入：**
```text
5
```
**輸出：**
```text
Area = 43.01193501472417
```
---

## 綜合範例 1-10：

請撰寫一程式，讓使用者輸入兩個正數 n、s，代表正 n 邊形之邊長為 s，計算並輸出此正 n 邊形之面積（Area）。

> 💡 **提示**：1：需 import math 模組，並使用 math.pow 及 math.tan。
> 💡 **提示**：2：正 n 邊形面積的公式如下：
   Area = (n * s²) / (4 * tan(pi/n))
> 💡 **提示**：3：輸出浮點數到小數點後第四位。

### 輸入與輸出樣本：

**輸入：**
```text
8
6
```
**輸出：**
```text
Area = 173.8234
```
---

## 綜合範例 1-11：

請撰寫一程式，請使用者輸入攝氏溫度，然後輸出其對應的華氏溫度。

> 💡 **提示**：華氏溫度 = (9 / 5) * 攝氏溫度 + 32。

### 輸入與輸出樣本：

**輸入：**
```text
100
```
**輸出：**
```text
Celsius 100.00
```
---> Fahrenheit 212.00


---

## 綜合範例 1-12：

請撰寫一程式，請使用者輸入三角形的三點座標，然後計算此三角形的面積。

> 💡 **提示**：假設三角形三個點的座標為 (x1, y1), (x2, y2), (x3, y3)，則三邊的長分別為 side1, side2, 以及 side3。利用
綜合範例 1-8
可得到這三邊的長。然後利用下列公式即可得到此三角形的面積。

s = (side1 + side2 + side3) / 2
area = √(s * (s - side1) * (s - side2) * (s - side3))

### 輸入與輸出樣本：

**輸入：**
```text
1.5, -3.4
4.6, 5
9.5, -3.4
```
**輸出：**
```text
The area of the triangle = 33.60
```
---

## 綜合範例 1-13：

請撰寫一程式，請使用者輸入矩形的長和寬，然後計算此矩形的面積和周長。

### 輸入與輸出樣本：

**輸入：**
```text
5, 10
```
**輸出：**
```text
area =  50
perimeter =  30
```
---

## 綜合範例 1-14：

平均加速度是兩個速度之差，除以時間。請撰寫一程式，請使用者輸入起始的速度 v0、結束的速度 v1，以及時間 t，然後顯示其平均速度。

❖ 計算公式：a = (v1 - v0)/t

### 輸入與輸出樣本：

**輸入：**
```text
5.8, 51.6, 4.6
```
**輸出：**
```text
average acceleration is 9.96
```
---

## 綜合範例 1-15：

假設你每個月在帳戶存款 10000，銀行的年利率為 1.23%，也就是說，月利率為 0.0123 / 12 = 0.001025。一個月後，帳戶的存款會是：

10000 * (1 + 0.001025) = 10010.25

兩個月過後，帳戶的存款會變成：

(10000 + 10010.25) * (1 + 0.001025) = 20030.76

三個月過後，帳戶的存款會變成：

(10000 + 20030.76) * (1 + 0.001025) = 30061.54

依此類推。

請撰寫一程式，提示使用者每個月存入帳戶的金額，並顯示六個月後帳戶裏的總金額。

### 輸入與輸出樣本：

**輸入：**
```text
Enter monthly saving amount: 10000
```
**輸出：**
```text
After the sixth month, the account value is 60215.61
```
---

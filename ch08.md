## 綜合範例 8-1
**題目敘述：**
請撰寫一程式，要求使用者輸入一字串，顯示該字串每個字元的索引。

### 輸入與輸出樣本：

**輸入：**
```text
Sandwich
```
**輸出：**
```text
Index of 'S': 0
Index of 'a': 1
Index of 'n': 2
Index of 'd': 3
Index of 'w': 4
Index of 'i': 5
Index of 'c': 6
Index of 'h': 7
```
---

## 綜合範例 8-2
**題目敘述：**
請撰寫一程式，要求使用者輸入一字串，顯示該字串每個字元的對應 ASCII 碼及其總和。

### 輸入與輸出樣本：

**輸入：**
```text
Kingdom
```
**輸出：**
```text
ASCII code for 'K' is 75
ASCII code for 'i' is 105
ASCII code for 'n' is 110
ASCII code for 'g' is 103
ASCII code for 'd' is 100
ASCII code for 'o' is 111
ASCII code for 'm' is 109
713
```
---

## 綜合範例 8-3
**題目敘述：**
請撰寫一程式，讓使用者輸入一個句子（至少有五個詞，以空白隔開），並輸出該句子倒數三個詞。

### 輸入與輸出樣本：

**輸入：**
```text
Many foreign students study in FJU
```
**輸出：**
```text
study in FJU
```
---

## 綜合範例 8-4
**題目敘述：**
請撰寫一程式，讓使用者輸入一字串，分別將該字串轉換成全部大寫以及每個字的第一個字母大寫。

### 輸入與輸出樣本：

**輸入：**
```text
learning python is funny
```
**輸出：**
```text
LEARNING PYTHON IS FUNNY
Learning Python Is Funny
```
---

## 綜合範例 8-5
**題目敘述：**
請撰寫一程式，要求使用者輸入一個長度為 6 的字串，將此字串分別置於 10 個欄位的寬度的左邊、中間和右邊，並顯示這三個結果，左右皆以直線 | (Vertical bar) 作為邊界。

### 輸入與輸出樣本：

**輸入：**
```text
python
```
**輸出：**
```text
|python    |
|  python  |
|    python|
```
---

## 綜合範例 8-6
**題目敘述：**
請撰寫一程式，讓使用者輸入一字串和一字元，並將此字串及字元作為參數傳遞給名為 compute() 的函式，此函式將回傳並輸出該字串中指定字元出現的次數。

### 輸入與輸出樣本：

**輸入：**
```text
Our country is beautiful
u
```
**輸出：**
```text
u occurs 4 time(s)
```
---

## 綜合範例 8-7
**題目敘述：**
請撰寫一程式，要求使用者輸入一字串，該字串為五個數字，以空白隔開。請將此五個數字加總（Total）並計算平均（Average）。

### 輸入與輸出樣本：

**輸入：**
```text
-2 34 18 29 -56
```
**輸出：**
```text
Total = 23
Average = 4.6
```
---

## 綜合範例 8-8
**題目敘述：**
請撰寫一程式，提示使用者輸入一個社會安全碼 SSN，格式為 ddd-dd-dddd，d 表示數字。若格式完全符合（正確的 SSN）則顯示【Valid SSN】，否則顯示【Invalid SSN】。

### 輸入與輸出樣本 1：

**輸入：**
```text
329-48-4977
```
**輸出：**
```text
Valid SSN
```
### 輸入與輸出樣本 2：

**輸入：**
```text
837-a3-3000
```
**輸出：**
```text
Invalid SSN
```
---

## 綜合範例 8-9
**題目敘述：**
請撰寫一程式，要求使用者輸入一個密碼（字串），檢查此密碼是否符合規則。密碼規則如下：

  a. 必須至少八個字元。
  b. 只包含英文字母和數字。
  c. 至少要有一個大寫英文字母。
  d. 若符合上述三項規則，程式將顯示檢查結果為【Valid password】，否則顯示【Invalid password】。

### 輸入與輸出樣本 1：

**輸入：**
```text
39Gfjkd98
```
**輸出：**
```text
Valid password
```
### 輸入與輸出樣本 2：

**輸入：**
```text
39dk8fh
```
**輸出：**
```text
Invalid password
```
---

## 綜合範例 8-10
**題目敘述：**
請撰寫一程式，首先要求使用者輸入正整數 k (1 <= k <= 100)，代表有 k 筆測試資料。每一筆測試資料是一串數字，每個數字之間以空白區隔，請找出此串列數字中最大值和最小值之間的差。

  > 💡 **提示**：差值輸出到小數點後第二位。

### 輸入與輸出樣本：

輸入與輸出會交雜如下，輸出之項目以粗體字表示
4
94 52.9 3.14 77 46
90.86
-2 0 1000.34 -14.4 89 50
1014.74
87.78 33333 29.3
33303.70
9998 9996 9999
3.00


---

## 綜合範例 8-11
**題目敘述：**
試撰寫一程式，以一不定迴圈要求使用者輸入字串，檢視若字串是以 B 字元開頭，則將此字串加入 lst 串列中，最後將其印出。當使用者輸入的 end 時將結束輸入的動作。

### 輸入與輸出樣本：

**輸入：**
```text
Block
Apple
Banana
Cathy
Boy
end
```
**輸出：**
```text
['Block', 'Banana', 'Boy']
```
---

## 綜合範例 8-12
**題目敘述：**
試撰寫一程式，以一不定迴圈要求使用者輸入字串，將輸入的字串以空白為分隔字元，並儲存於 lst 串列中，最後將其印出。

### 輸入與輸出樣本：

I am a teacher.
['I', 'am', 'a', 'teacher.']
He is a student
['He', 'is', 'a', 'student']
Hello, world
['Hello,', 'world']
end


---

## 綜合範例 8-13
**題目敘述：**
試撰寫一程式，隨機產生 10 個介於 65~90 的亂數，然後將其轉換為一對應於英文字母的字串。

### 輸入與輸出樣本 1：

**輸入：**
```text
無
```
**輸出：**
```text
YCHUOPDQDJ
```
### 輸入與輸出樣本 2：

**輸入：**
```text
無
```
**輸出：**
```text
QQGGNXCZCZ
```
---

## 綜合範例 8-14
**題目敘述：**
試撰寫一程式，輸入九個字串置放於一名為 lst 的字串，其長度不超過 10 個字元。接下來，每一列印出三個字串，並且向中靠齊。

### 輸入與輸出樣本：

**輸入：**
```text
apple
orange
kiwi
banana
grape
pineapple
guava
cherry
blueberry
```
**輸出：**
```text
|  apple   ||  orange  ||   kiwi   |
|  banana  ||  grape   ||pineapple |
|  guava   ||  cherry  ||blueberry |
```
---

## 綜合範例 8-15
**題目敘述：**
試撰寫一程式，輸入一名為 str 的字串與欲尋找的字串，將找到的字串以 'Bright' 字串取代之。若沒有欲尋找的字串，則印出 'not found' 的訊息。

### 輸入與輸出樣本：

**輸入：**
```text
I make an appointment with Linda
Linda
```
**輸出：**
```text
I make an appointment with Bright
```
---

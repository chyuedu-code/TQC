## 綜合範例 2-1：

請使用選擇敘述撰寫一程式，讓使用者輸入一個正整數，然後判斷它是否為偶數（even）。

### 輸入與輸出樣本 1：

**輸入：**
```text
56
```
**輸出：**
```text
56 is an even number.
```
### 輸入與輸出樣本 2：

**輸入：**
```text
21
```
**輸出：**
```text
21 is not an even number.
```
---

## 綜合範例 2-2：

請使用選擇敘述撰寫一程式，讓使用者輸入一個正整數，然後判斷它是 3 或 5 的倍數，若此數值同時為 3 與 5 的倍數，顯示【x is a multiple of 3 and 5.】；如此數值皆不屬於 3 或 5 的倍數，顯示【x is not a multiple of 3 or 5.】，將使用者輸入的數值代入 x。

### 輸入與輸出樣本 1：

**輸入：**
```text
55
```
**輸出：**
```text
55 is a multiple of 5.
```
### 輸入與輸出樣本 2：

**輸入：**
```text
36
```
**輸出：**
```text
36 is a multiple of 3.
```
### 輸入與輸出樣本 3：

**輸入：**
```text
92
```
**輸出：**
```text
92 is not a multiple of 3 or 5.
```
### 輸入與輸出樣本 4：

**輸入：**
```text
15
```
**輸出：**
```text
15 is a multiple of 3 and 5.
```
---

## 綜合範例 2-3：

請使用選擇敘述撰寫一程式，讓使用者輸入一個西元年份，然後判斷它是否為閏年（leap year）或平年。其判斷規則為：每四年一閏，每百年不閏，但每四百年也一閏。

### 輸入與輸出樣本 1：

**輸入：**
```text
1992
```
**輸出：**
```text
1992 is a leap year.
```
### 輸入與輸出樣本 2：

**輸入：**
```text
2010
```
**輸出：**
```text
2010 is not a leap year.
```
---

## 綜合範例 2-4：

請使用選擇敘述撰寫一程式，讓使用者輸入兩個整數 a、b，然後再輸入一算術運算子（+、-、*、/、//、%），輸出這兩個數以及其經過運算後的結果。

### 輸入與輸出樣本：

**輸入：**
```text
30
20
*
```
**輸出：**
```text
600
```
---

## 綜合範例 2-5：

請使用選擇敘述撰寫一程式，讓使用者輸入一個字元，判斷它是包括大、小寫的英文字母（alphabet）、數字（number）、或者其它字元（symbol）。例如：a 為英文字母、9 為數字、$ 為其它字元。

### 輸入與輸出樣本 1：

**輸入：**
```text
P
```
**輸出：**
```text
P is an alphabet.
```
### 輸入與輸出樣本 2：

**輸入：**
```text
@
```
**輸出：**
```text
@ is a symbol.
```
### 輸入與輸出樣本 3：

**輸入：**
```text
7
```
**輸出：**
```text
7 is a number.
```
---

## 綜合範例 2-6：

請使用選擇敘述撰寫一程式，根據使用者分數顯示對應的等級。

標準如下表所示：

分數      等級
80 ~ 100  A
70 ~ 79   B
60 ~ 69   C
<= 59     F

### 輸入與輸出樣本：

**輸入：**
```text
79
```
**輸出：**
```text
B
```
---

## 綜合範例 2-7：

**題目敘述：**
請使用選擇敘述撰寫一程式，要求使用者輸入購物金額，購物金額需大於 8,000（含）以上，並顯示折扣優惠後的實付金額。

購物金額折扣方案如下表所示：

金額          折扣
8,000 (含) 以上  9.5 折
18,000 (含) 以上 9 折
28,000 (含) 以上 8 折
38,000 (含) 以上 7 折

### 輸入與輸出樣本：

**輸入：**
```text
12000
```
**輸出：**
```text
11400.0
```
---

## 綜合範例 2-8：

**題目敘述：**
請使用選擇敘述撰寫一程式，讓使用者輸入一個十進位整數 num(0 <= num <= 15)，將 num 轉換成十六進位值。

> 💡 **提示**：轉換規則 = 十進位 0~9 的十六進位值為其本身，十進位 10~15 的十六進位值為 A~F。

### 輸入與輸出樣本 1：

**輸入：**
```text
13
```
**輸出：**
```text
D
```
### 輸入與輸出樣本 2：

**輸入：**
```text
8
```
**輸出：**
```text
8
```
---

## 綜合範例 2-9：

**題目敘述：**
請使用選擇敘述撰寫一程式，讓使用者輸入一個點的平面座標 x 和 y 值，判斷此點是否與點 (5, 6) 的距離小於或等於 15，如距離小於或等於 15 顯示【Inside】，反之顯示【Outside】。

> 💡 **提示**：計算平面上兩點距離的公式：$\sqrt{(x_1 - x_2)^2 + (y_1 - y_2)^2}$。

### 輸入與輸出樣本 1：

**輸入：**
```text
7
20
```
**輸出：**
```text
Inside
```
### 輸入與輸出樣本 2：

**輸入：**
```text
30
35
```
**輸出：**
```text
Outside
```
---

## 綜合範例 2-10：

**題目敘述：**
請使用選擇敘述撰寫一程式，讓使用者輸入三個邊長，檢查這三個邊長是否可以組成一個三角形。若可以，則輸出該三角形之周長；否則顯示【Invalid】。

> 💡 **提示**：檢查方法 = 任意兩個邊長之總和大於第三邊長。

### 輸入與輸出樣本 1：

**輸入：**
```text
5
6
13
```
**輸出：**
```text
Invalid
```
### 輸入與輸出樣本 2：

**輸入：**
```text
1
1
1
```
**輸出：**
```text
3
```
---

## 綜合範例 2-11：

**題目敘述：**
請使用選擇敘述撰寫一程式，由使用者的點座標 (x, y)，然後檢視該點是否位於中心點為 (0, 0)，長為 8，高為 6 的矩形內。

> 💡 **提示**：如果此點與矩形中心點之水平距離小於或等於 8 / 2，而且垂直距離小於或等於 6 / 2，則此點位於矩形內，否則位於矩形外。

### 輸入與輸出樣本 1：

**輸入：**
```text
4, 4
```
**輸出：**
```text
(4, 4) is outside of the rectangle
```
### 輸入與輸出樣本 2：

**輸入：**
```text
4, 3
```
**輸出：**
```text
(4, 3) is inside of the rectangle
```
---

## 綜合範例 2-12：

**題目敘述：**
請使用選擇敘述撰寫一程式，利用亂數產生器產生介於 1~100 之間的亂數，然後檢視這個亂數是偶數或是奇數。

> 💡 **提示**：如果此亂數除以 2，餘數為 0 時，則為偶數，否則為奇數。

### 輸入與輸出樣本 1：

**輸入：**
```text
無
```
**輸出：**
```text
6 is even number.
```
### 輸入與輸出樣本 2：

**輸入：**
```text
無
```
**輸出：**
```text
35 is odd number.
```
---

## 綜合範例 2-13：

**題目敘述：**
請使用選擇敘述撰寫一程式，利用克拉瑪（Cramer's rule）公式解二元一次方程式。假設有二個二元一次方程式，如下所示：

ax + by = c
dx + ey = f

x 與 y 的解如下：

x = (ce - bf) / (ae - bd)
y = (af - cd) / (ae - bd)

> 💡 **提示**：如果 (ae - bd) 為 0，則表示無解。

### 輸入與輸出樣本 1：

**輸入：**
```text
input a, b, c: 1, 2, 4
input d, e, f: 2, 4, 5
```
**輸出：**
```text
No solution.
```
### 輸入與輸出樣本 2：

**輸入：**
```text
input a, b, c: 9, 4, -6
input d, e, f: 3, -5, -21
```
**輸出：**
```text
x is -2, y = 3.
```
---

## 綜合範例 2-14：

**題目敘述：**
請使用選擇敘述撰寫一程式，讓使用者輸入的三位數的整數，檢視它是否為迴文（palindrome number）。

### 輸入與輸出樣本 1：

**輸入：**
```text
131
```
**輸出：**
```text
131 is a palindrome number.
```
### 輸入與輸出樣本 2：

**輸入：**
```text
122
```
**輸出：**
```text
122 is not a palindrome number.
```
---

## 綜合範例 2-15：

**題目敘述：**
請使用選擇敘述撰寫一程式，輸入三個整數，並由小至大加以排序後印出。

### 輸入與輸出樣本：

**輸入：**
```text
Enter three integers: 8, 6, 1
```
**輸出：**
```text
The sorted numbers are 1 6 8
```
---
